import React, { useState } from 'react'

export const usePaginate = () => {

    const [data, setData] = useState({
        data :[],
        page: 0,
        nextPage: 0,
        prevPage: 0,
        limit: 0,
        total: 0
})
return data
  return (
    usePaginate
  )
}
